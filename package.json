{
  "name": "myntra-wishlist-discovery",
  "version": "1.0.0",
  "private": true,
  "description": "Myntra Trust-Verified Review System MVP",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npx prisma generate",
    "start": "cd backend && npx prisma generate && npx prisma db push && node prisma/seed.js && node src/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
